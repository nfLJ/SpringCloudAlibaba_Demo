
spring:
  application:
    # 应用名称
    name: service-config

  cloud:
    # 使用 Naoos 作为服务注册发现
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        file-extension: yml
  profiles:
    active: prod




